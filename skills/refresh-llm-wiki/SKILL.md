# refresh-llm-wiki

Inspect the repository, then update the smallest set of pages under
`docs/llm/` whose source code or behavior changed. Keep generated content
deterministic and update the index when pages are added or removed.

The implementation belongs in `scripts/ai/refresh.js`.
