# Setup Codebase

Establish agent context before implementation. `AGENTS.md` is the canonical
contract; `CLAUDE.md` redirects compatible agents to it. This wiki records
source-linked summaries rather than replacing the codebase.

## Sources

- `AGENTS.md`
- `docs/llm/AGENTS.md`
